/**
 * main - print the name number of argument passe to it
 * @argc: number of arguments passed to the function
 * @argv: argument vector of pointer to strings
 *
 * Return: always 0
 */
int main(int argc, char *argv[] __attribute__((unuse))
{
	printf("%d\n",argc -1);
	return(0);
}
